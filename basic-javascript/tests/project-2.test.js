/* eslint-disable prefer-destructuring */

const assert = require('chai').assert;
const funcs = require('../src/project-2');

// whoops.. there is no test suite for this file. You'll simply just have to create one :/
// COMPLETED: by DANTE

describe('Project-2 Functions', () => {
  describe('getBiggest', () => {
    it('should be a function', () => {
      const func = funcs.getBiggest;
      assert.typeOf(func, 'function');
    });
  });

  describe('greeting', () => {
    it('should be a function', () => {
      const func = funcs.greeting;
      assert.typeOf(func, 'function');
    });
  });

  describe('isTenOrFive', () => {
    it('should be a function', () => {
      const func = funcs.isTenOrFive;
      assert.typeOf(func, 'function');
    });
  });

  describe('isInRange', () => {
    it('should be a function', () => {
      const func = funcs.isInRange;
      assert.typeOf(func, 'function');
    });
  });

  describe('isInteger', () => {
    it('should be a function', () => {
      const func = funcs.isInteger;
      assert.typeOf(func, 'function');
    });
  });

  describe('fizzBuzz', () => {
    it('should be a function', () => {
      const func = funcs.fizzBuzz;
      assert.typeOf(func, 'function');
    });
  });

  describe('isPrime', () => {
    it('should be a function', () => {
      const func = funcs.isPrime;
      assert.typeOf(func, 'function');
    });
  });

  describe('returnFirst', () => {
    it('should be a function', () => {
      const func = funcs.returnFirst;
      assert.typeOf(func, 'function');
    });
  });

  describe('returnLast', () => {
    it('should be a function', () => {
      const func = funcs.returnLast;
      assert.typeOf(func, 'function');
    });
  });

  describe('getArrayLength', () => {
    it('should be a function', () => {
      const func = funcs.getArrayLength;
      assert.typeOf(func, 'function');
    });
  });

  describe('incrementByOne', () => {
    it('should be a function', () => {
      const func = funcs.incrementByOne;
      assert.typeOf(func, 'function');
    });
  });

  describe('addItemToArray', () => {
    it('should be a function', () => {
      const func = funcs.addItemToArray;
      assert.typeOf(func, 'function');
    });
  });

  describe('addItemToFront', () => {
    it('should be a function', () => {
      const func = funcs.addItemToFront;
      assert.typeOf(func, 'function');
    });
  });

  describe('wordsToSentence', () => {
    it('should be a function', () => {
      const func = funcs.wordsToSentence;
      assert.typeOf(func, 'function');
    });
  });

  describe('contains', () => {
    it('should be a function', () => {
      const func = funcs.contains;
      assert.typeOf(func, 'function');
    });
  });

  describe('addNumbers', () => {
    it('should be a function', () => {
      const func = funcs.addNumbers;
      assert.typeOf(func, 'function');
    });
  });

  describe('averageTestScore', () => {
    it('should be a function', () => {
      const func = funcs.averageTestScore;
      assert.typeOf(func, 'function');
    });
  });

  describe('largestNumber', () => {
    it('should be a function', () => {
      const func = funcs.largestNumber;
      assert.typeOf(func, 'function');
    });
  });
});
