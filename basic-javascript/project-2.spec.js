const funcs = require('./project-2');

// whoops.. there is no test suite for this file. You'll simply just have to create one :/
describe('project-2', () => {
    describe('getBiggest', () => {
        it('returns NaN when given a non numeric value', () => {
            //expect(helpers.getBiggest(undefined, undefined)).toBeNaN();
            //expect(helpers.getBiggest("two", "two")).toBeNaN();
        
        })
        it('should return largest number', () => {
           // expect(helpers.getBiggest(9, 10)).toEqual(10);
            //expect(helpers.getBiggest(9.5, 10.2)).toEqual(10.2);
            //expect(helpers.getBiggest(-1, 0)).toEqual(0);
            //expect(helpers.getBiggest(0, 0)).toEqual(0);
        });
    })
})